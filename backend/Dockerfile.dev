FROM golang:1.25-alpine

WORKDIR /app
ENV GOFLAGS=-mod=mod

COPY backend/go.mod backend/go.sum* ./
RUN go mod download
COPY backend/ ./
RUN go build -o /usr/local/bin/erp-api ./cmd/api

EXPOSE 8000

CMD ["erp-api"]
